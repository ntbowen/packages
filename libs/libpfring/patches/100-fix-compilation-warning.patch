--- a/kernel/pf_ring.c
+++ b/kernel/pf_ring.c
@@ -4109,7 +4109,7 @@ static int hash_pkt_cluster(ring_cluster_e
       flags |= mask_5_tuple;
       break;
     } /* else use 2-tuple for non-TCP packets */
-    /* fall through */
+    fallthrough;

   case cluster_per_flow_2_tuple:
   case cluster_per_inner_flow_2_tuple:
