--- a/cmake/LwsCheckRequirements.cmake
+++ b/cmake/LwsCheckRequirements.cmake
@@ -40,23 +40,15 @@ MACRO(require_lws_config reqconfig _val result)
 	else()
 		if (LWS_WITH_MINIMAL_EXAMPLES)
 			set(MET ${SAME})
 		else()
-			CHECK_C_SOURCE_COMPILES("#include <libwebsockets.h>\nint main(void) {\n#if defined(${reqconfig})\n return 0;\n#else\n fail;\n#endif\n return 0;\n}\n" HAS_${reqconfig})
-			if (NOT DEFINED HAS_${reqconfig} OR NOT HAS_${reqconfig})
-				set(HAS_${reqconfig} 0)
-			else()
-				set(HAS_${reqconfig} 1)
-			endif()
-			if ((HAS_${reqconfig} AND ${_val}) OR (NOT HAS_${reqconfig} AND NOT ${_val}))
-				set(MET 1)
-			else()
-				set(MET 0)
-			endif()
+			set(MET ${SAME})
 		endif()
 		if (NOT MET AND _cmp)
 			if (${_val})
-				message(FATAL_ERROR "This project requires lws must have been configured with ${reqconfig}")
+				message(WARNING "This project requires lws must have been configured with ${reqconfig}, skipping")
+				set(${result} 0)
 			else()
-				message(FATAL_ERROR "Lws configuration of ${reqconfig} is incompatible with this project")
+				message(WARNING "Lws configuration of ${reqconfig} is incompatible with this project, skipping")
+				set(${result} 0)
 			endif()
 		endif()
